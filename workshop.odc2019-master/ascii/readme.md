To install the virtualenv and pillow:

sudo aptitude install virtualenv
virtualenv -p python3 ~/home/venvs/pil
. ~/home/venvs/pil/bin/activate
pip install pillow

Then to run the script:
pip install pillow
python line.py